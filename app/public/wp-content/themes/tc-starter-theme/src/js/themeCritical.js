import {tcAppendStyleSheet,  tcLoader} from "./tc-modules";
//----------------------------------------------------------------------------------------------------------------------
//----------------------------------------------------------------------------------------------------------------------
//LOADER
//----------------------------------------------------------------------------------------------------------------------
//----------------------------------------------------------------------------------------------------------------------

let tasks = [
    initRenderBlockingResources
]
tcLoader(tasks);

function initRenderBlockingResources() {
    tcAppendStyleSheet(tcThemeAID.css_url + '/theme.min.css', 'theme-css')
 }
